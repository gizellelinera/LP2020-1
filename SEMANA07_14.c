/*
#include <stdio.h>
#include <stdlib.h>

int main() {
  int atual=0,num;

num=0;
	while(num>=0){
    scanf("%i",&num);
	  if(num>atual && num>=0)
			printf("C\n");
  	else
	  	if(num<atual && num>=0)
		  	printf("B\n");
	    else
		    if(num==atual)
			    printf("-\n");
		atual=num;
	}
  return 0;
}
*/